/**
 * @module string/charAt
 * Utility function for advanced string charAt manipulation
 */
export const charAtUtility = (input: string): string | boolean | number => {
  if (typeof input !== 'string') return '';
  // Implementation specific to charAt
  return input;
};
