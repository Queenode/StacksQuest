/**
 * @module string/trimEnd
 * Utility function for advanced string trimEnd manipulation
 */
export const trimEndUtility = (input: string): string | boolean | number => {
  if (typeof input !== 'string') return '';
  // Implementation specific to trimEnd
  return input;
};
