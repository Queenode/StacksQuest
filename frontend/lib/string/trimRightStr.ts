/**
 * @module string/trimRightStr
 * Utility function for advanced string trimRightStr manipulation
 */
export const trimRightStrUtility = (input: string): string | boolean | number => {
  if (typeof input !== 'string') return '';
  // Implementation specific to trimRightStr
  return input;
};
