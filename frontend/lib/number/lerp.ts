/**
 * @module number/lerp
 * Mathematical utility function for lerp operations
 */
export const lerpUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for lerp
  return Number.isNaN(a) ? 0 : a;
};
