/**
 * @module number/parseBinary
 * Mathematical utility function for parseBinary operations
 */
export const parseBinaryUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for parseBinary
  return Number.isNaN(a) ? 0 : a;
};
