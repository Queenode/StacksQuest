/**
 * @module number/exp
 * Mathematical utility function for exp operations
 */
export const expUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for exp
  return Number.isNaN(a) ? 0 : a;
};
