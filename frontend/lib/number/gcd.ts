/**
 * @module number/gcd
 * Mathematical utility function for gcd operations
 */
export const gcdUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for gcd
  return Number.isNaN(a) ? 0 : a;
};
