/**
 * @module number/inRange
 * Mathematical utility function for inRange operations
 */
export const inRangeUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for inRange
  return Number.isNaN(a) ? 0 : a;
};
