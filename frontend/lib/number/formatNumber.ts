/**
 * @module number/formatNumber
 * Mathematical utility function for formatNumber operations
 */
export const formatNumberUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for formatNumber
  return Number.isNaN(a) ? 0 : a;
};
