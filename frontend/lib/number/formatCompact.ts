/**
 * @module number/formatCompact
 * Mathematical utility function for formatCompact operations
 */
export const formatCompactUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for formatCompact
  return Number.isNaN(a) ? 0 : a;
};
