/**
 * @module number/toBinary
 * Mathematical utility function for toBinary operations
 */
export const toBinaryUtility = (a: number, b?: number): number | boolean | string => {
  if (typeof a !== 'number') return 0;
  // Stub implementation for toBinary
  return Number.isNaN(a) ? 0 : a;
};
